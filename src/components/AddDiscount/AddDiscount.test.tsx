import { render, screen } from "@testing-library/react"
import { describe } from "vitest"
import Discount from "./AddDiscount"

describe("Test Cases", async () => {
    // beforeEach(() => {
    //     render(<FeeStructure />)
    // })

    it('Dummy', () => {
        expect(0).toBe(0)
    })
})
