import { render, screen } from "@testing-library/react"
import { describe } from "vitest"
import FeeStructure from "./FeeCategory"

describe("Test Cases", async () => {
    // beforeEach(() => {
    //     render(<FeeStructure />)
    // })

    it('Dummy', () => {
        expect(0).toBe(0)
    })
})
